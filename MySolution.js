class MySolution {

    constructor(a, b) {
        this.a = a;
        this.b = b;
    }

    sum(b, a) { return a + b; }
    value() { return this.a; }
    test(a,b) { return a + b; }
}

export default MySolution;
